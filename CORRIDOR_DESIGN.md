# 三十五亿年时间走廊 —— 架构决策（ADR）

## 核心裁决：时间轴上的真源是数据，不是视图

**服务端数据是唯一真源（Source of Truth）；前端视图只是数据的一份可分享投影。**

1. **事实只在服务端产生。** 标本的年代区间、分类、氧需求、形态描述，事件与标本的关联，
   大氧化/大灭绝的判定，区间检索结果，计数与哈希——全部由服务端计算并版本化。
   前端不允许根据本地数据自行得出任何"结论"（不准自己算氧够不够、不准自己关联灭绝事件）。
2. **视图状态只有两个量：`at`（时间点，百万年，带符号，今为 0）与 `ppm`
   （每像素百万年数，即缩放）。** 它是一个"书签坐标"，写进地址栏：
   `/corridor?at=-2400&ppm=0.3`。任何人打开同一 URL，服务端返回同一批数据，
   看到的就是同一屏。视图状态不包含任何事实，丢了随时可以从 URL 重新投影出来。
3. **确定性可断言。** 同一窗口/同一年代的标本集合与数量恒定：集合按
   `(firstMa 升序, id 字典序)` 排序，载荷附带规则版本与 SHA-256 哈希，可直接写进测试断言。
4. **反事实推演是服务端的版本化规则引擎。** "如果它早出现十亿年"由固定顺序的规则
   R0–R5 给出阻断/存活结论与完整 trace，同输入永远同输出；前端只负责提问和展示。
5. **竞态的边界划在数据层。** 视图可以随意拖动/缩放（每帧重绘刻度是纯投影），
   但服务端数据图层的"提交"必须经过世代令牌（generation token）：
   缩放层级切换时，迟到的旧响应只能被丢弃，永远不能覆盖已画好的新图层；
   新图层就绪前继续绘制旧图层（图层不被清空）。naive/guarded 两种加载器并存，
   可复现对照（见走廊页"竞态实验室"与 `tests/timeline.test.ts`）。

## 坐标约定

- 时间单位 Ma（百万年），**带符号**：今 = 0，35 亿年前 = -3500。例：`at=-2400`。
- 区间一律 `[firstMa, lastMa]`（含端点），`firstMa <= lastMa`；0 表示延续至今。
- 走廊范围：[-3500, 0]；反事实前移超过 -3500 由规则 R0 判为 invalid。

## HTTP 接口（全部服务端计算）

| 方法 | 路径 | 真源内容 |
|---|---|---|
| GET | `/api/timeline/meta` | 边界、数据集版本、全部事件、氧曲线、宙/代/纪带 |
| GET | `/api/timeline/window?from&to` | 区间查询：标本（含泳道与关联事件）、事件、年代带；带 count/hash |
| GET | `/api/timeline/era?at` | 某年的宙/代/纪、标本 id 集合与数量、哈希（可断言） |
| GET | `/api/timeline/specimen/:id` | 标本详情与"当时模样"（形态重建） |
| POST | `/api/timeline/counterfactual` | `{id, shiftMa}` → R0–R5 确定性反事实推演 |
| GET | `/api/timeline/index-stats` | 服务端区间索引（中心区间树）的统计 |

## 性能模型

- 刻度全集（1 Ma 一格 = 3501 格）由纯函数生成；每帧只对可见区间做一次二分裁剪，
  千格以上用单条 Path2D 批量描线，拖动走 requestAnimationFrame，目标 60fps（HUD 实测显示）。
- 标本/事件/氧曲线来自服务端窗口载荷，前端只做几何投影与绘制，不做事实计算。
